# Historical implementation notes

This directory is retained as an archive of the policy-engine prototype. It is
not an active progress tracker. See the repository `README.md`, the
`agent_smith` connector package, and executable tests for current behavior.
